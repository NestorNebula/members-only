<nav>
  <ul>
    <% if (user) { %>
    <li class="navlink left">
      <% if (!user.member) { %>
      <a href="/join">Join The Club</a>
      <% } else if (!user.admin) { %>
      <a href="/admin">Become an Admin</a>
      <% } %>
    </li>
    <li class="navlink center"><a href="/">The Club</a></li>
    <li class="navlink right"><a href="/log-out">Log Out</a></li>
    <% } else { %>
    <li class="navlink left"><a href="/sign-up">Sign-Up</a></li>
    <li class="navlink center"><a href="/">The Club</a></li>
    <li class="navlink right"><a href="/log-in">Log-In</a></li>
    <% } %>
  </ul>
</nav>