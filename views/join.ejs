<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Join The Club</title>
</head>
<body>
  <% if (locals.error) { %>
    <div><%= error %></div>
  <% } %>
  <div>So, <%= user.first_name %> you wanna become a member of "The Club?"</div>
  <div>To have this unique privilege, please add the secret password that one of the club members gave you.</div>
  <form action="/join" method="POST">
    <label for="password">Password</label>
    <input id="password" name="password" type="password">
    <button>Submit</button>
  </form>
</body>
</html>